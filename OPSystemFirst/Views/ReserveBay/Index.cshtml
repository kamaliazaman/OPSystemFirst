@model IEnumerable<OPSystemFirst.Models.ReserveBayAdaptee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ParkingAdapter.parkingType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SignUp.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lotAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.startDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.endDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ParkingAdapter.parkingType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SignUp.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lotAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.startDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.endDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReserveBayAdapteeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReserveBayAdapteeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReserveBayAdapteeId })
        </td>
    </tr>
}

</table>
